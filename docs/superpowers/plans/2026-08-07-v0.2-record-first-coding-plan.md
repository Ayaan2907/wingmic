# v0.2 coding plan — record-first UX → trust → imports → public graph → acts

**Date:** 2026-08-07 · **Status:** ready for execution · **Base:** `main` @ `990c382`  
**Audience:** Cursor executor. Coding work packages first; architecture only where a WP needs a locked decision.  
**Continues:** readiness audit `2026-08-07-readiness-audit.md` + Framing D in `docs/architecture.md`.  
**Epics:** trust (new) · imports #10 · acts #11 · public enrichment (new, under Framing D).

---

## 0 · Product north star (one paragraph)

After sign-in, the user should feel **one verb**: record. Tap → speak → done → **immediately ready to record again**. Extraction, graph writes, and later acts happen **in the background**; the UI shows progress without blocking the mic. Imports cold-start people so spoken names resolve to real contacts. Companies/events stay Framing D canonical (shared, lazy-promoted); optional public enrichers (domain / Luma / Meetup) attach later. Acts turn extracted follow-ups into permission-first drafts. **Ship order is UX honesty → data truth → imports → enrichment → acts** — not all at once in one PR.

---

## 1 · UX expert pass — simplify the feel

### 1.1 What confuses today
- Home **acts drafts are intentional UI** (the Acts surface) — but CTAs are still disabled / seeded, so they feel broken rather than unfinished.
- Jargon (`commits`, `edges`) without a clear first verb (“tap to talk”).
- Person pages forget the memo that created them (`source_interaction_id` null) — T1 fixes new commits.
- Desktop rails show mock people next to real data.
- Next recording can feel gated by pipeline state / route churn.

### 1.2 Target feel (acceptance, not mock pixels)
1. **Post-login destination = `/chat` with mic ready** (home stays as glance + **working** acts queue). Onboarding still gates once; after acknowledge → chat (or home if user prefers — Q1).
2. **Record loop:** stop → orb returns to idle **immediately** → user can start again while prior bubbles show `transcribing → linking → committed` in the thread.
3. **Background is the product:** no modal “please wait to record again”; failures stay on that bubble with retry/paste.
4. **Home acts drafts stay — and become real.** Same `ActCard` chrome on home + `/acts`. Data comes from extracted memo actions (email / meeting / reminder / intro / todo), not hardcoded Sarah/Marcus seeds. Empty only when the user truly has no pending acts.
5. **One teaching beat:** first session shows “tap to talk” on the orb once; then silence.

### 1.3 Coding WPs — UX (do after T1 so person pages tell the truth)

| WP | Change | Files (exact) | Tests |
|---|---|---|---|
| **U1** | Post-onboarding / sign-in default → `/chat` (callbackURL + home redirect if onboarded) | `SignInClient.tsx`, `onboarding` success path, `app/page.tsx` gate | existing onboarding/signin tests + one redirect assert |
| **U2** | Home acts block: swap seeds for live `acts.list` (pending); recent rows link to `/chat` or entity; `pendingActs` count real | `HomeClient.tsx`, `app/page.tsx` | `HomeClient.test.tsx` with mocked acts |
| **U3** | Capture loop: ensure orb idle as soon as recorder stops; pipeline never blocks `armRecord` / tap-toggle | `CaptureProvider.tsx`, `useAudioRecorder.ts`, `BottomTabBar.tsx` | ChatClient tests: start second record while first is `linking` |
| **U4** | Strip or gate mock rails (`ChatEntityRail`, `PersonListRail`) — hide until wired to live thread/graph data | those components + parents | render tests: no fixture people when live data present |
| **U5** | First-run orb hint (localStorage once) | `BottomTabBar` / `AppShell` | unit for hint dismiss |

**Do not** rename brand jargon (`commit`) in this phase — teach via empty states / hint, not a glossary rewrite.

---

## 2 · Trust pack (ship first — already approved)

One PR per WP. Target `staging`. Gate: `bun run typecheck && bun run lint && bun run test`.

| WP | Issue | Work | Primary files |
|---|---|---|---|
| **T1** | file `[fix](extractor): stamp sourceInteractionId on facts/topics` | Create `interaction` **before** fact/topic inserts; pass `sourceInteractionId`. Person detail then shows captures. | `packages/extractor/src/resolution.ts` + new `resolution.test.ts` |
| **T2** | file `[feat](search): deep-link results to entity pages` | Result card → `/person|company|event/[id]` | `SearchClient.tsx` + test |
| **T3** | ~~remove acts seeds~~ **superseded** — see **A1–A4** (wire home/`/acts` drafts to work; do not delete the UI) | — | — |
| **T4** | same as U4 or fold | Mock rails out | chat/person rails |
| **T5** | file `[feat](capture): persist soft-delete` | `capture.delete` tRPC + provider calls it; undo restores | `capture.ts` router, `CaptureProvider`, schema already has `interactions.deletedAt` |

**T1 detail (locked coding steps):**
1. In `commit()`, after embeddings, `insert` interaction → `interactionId`.
2. Pass `sourceInteractionId: interactionId` into every `entityFacts` + `entityTopics` insert.
3. Vitest with in-memory libSQL (mirror `apps/app` router tests) + mocked `embedText`/`embedTexts`: assert fact/topic rows have non-null `source_interaction_id`; `entity.detail` captures length ≥ 1.
4. Commit: `fix(extractor): stamp sourceInteractionId on facts and topics`.

---

## 3 · Version map (complete release, broken into code)

Think of the “next version” as **four stacked releases**, not one mega-PR. Each column is independently shippable to `staging` → `main`.

```
v0.1.3  Trust + record-first UX     (T1, T2, T4, T5, U1, U3–U5)
v0.2    Contact imports + resolve     (#10 expanded)
v0.2.1  Public company/event enrich   (Framing D + connectors)
v0.3    Acts live — wire existing draft UI (#11)   ← home ActCards + /acts stay
```

**Acts timing note (user direction 2026-08-07):** the home/`/acts` draft cards were put there on purpose. Do **not** rip them out for an empty state. Pull **A1–A4 forward** so those cards show real drafts from memo `actions[]` (email, meeting/calendar invite, reminder, intro, todo) with permission-first send (`mailto:` / calendar download — no auto-send). Seeds remain only as fixture data in tests, never in production UI once A2 lands.

Twitter/X: treat as **best-effort / optional** — API access is restricted; do not block v0.2 on it. Prefer LinkedIn CSV + device contacts + Google People first.

---

## 4 · v0.2 Imports — coding plan (before deep design)

**Reuse existing schema:** `entity.import_source`, `entity_fact` keys (`email`, `linkedin`, …), `identity_claim` (dormant → wire), Framing D private people.

### 4.1 Package / app layout (primitives, no wrappers)

```
packages/imports/          # NEW only if roadmap allows — else keep parsers in apps/app/lib/imports/
  src/
    parseLinkedInCsv.ts    # pure
    parseVcard.ts          # pure
    normalizeContact.ts    # → ImportContactDraft Zod
apps/app/
  app/imports/             # UI: upload / device / OAuth callback
  lib/trpc/routers/imports.ts
```

Check README roadmap before adding `packages/imports`. Default for first PR: **`apps/app/lib/imports/*` pure modules** + tRPC; hoist to package only if web needs it too (it won't).

### 4.2 Work packages

| WP | Title | Coding notes |
|---|---|---|
| **I0** | Issue tree under #10 | Sub-issues for CSV, vCard, Google, device contacts, resolve-on-capture, badges, undo |
| **I1** | Zod `ImportContactDraft` + LinkedIn CSV parser | Pure functions + vitest fixtures (fake names only) |
| **I2** | `imports.upsertBatch` tRPC | userId-scoped entity create/match by email/linkedin/name; set `importSource`; no cross-user leak test |
| **I3** | `/imports` UI — file drop for LinkedIn CSV + vCard | Progress for 500 rows < 2 min; brand empty states |
| **I4** | Device contacts | Contact Picker API where supported; fallback = vCard upload from phone |
| **I5** | Google People OAuth | BetterAuth social / Google provider; map to same upsert |
| **I6** | Resolve-on-capture | In `capture.commit` / `resolution.ts`: pass `knownContacts` **enriched with import aliases + emails**; prefer match to imported entity (already partially via knownContacts names — extend with import embeddings / email facts) |
| **I7** | Import badges on person UI | `EntityDetailScaffold` chip from `importSource` |
| **I8** | Undo import | Soft-delete entities with that `importSource` batch id (may need `import_batch_id` column — **append-only migration** if so) |
| **I9** | Twitter/X (optional) | Spike only: public profile URL paste → fact `twitter`; full graph import likely blocked — document in issue, do not promise |

**Storage decision (coding-locked, Framing D):**
- **People contacts:** store **your** copy as private `entity` rows (+ facts). Do **not** scrape full social graphs into a global people table.
- **Public profile fields** (company domain, LinkedIn URL): store as facts on **your** entity; company row stays canonical shared.
- **Map vs store:** store enough to resolve offline (name, email, urls). Enrich later by URL fetch only where ToS allows; never commit scraped bios into other users’ graphs.

---

## 5 · Public companies / semi-public events — coding plan

Already in schema: `company.observed_count` / `promoted_at`, `event` same. **#2 lazy-promotion** is the unfinished code path — do it before external connectors.

| WP | Title | Notes |
|---|---|---|
| **P1** | Finish #2 lazy-promotion side effects | `upsertCompany` / `upsertEvent` industry/location/mode per issue #2 |
| **P2** | Domain → company enrich | Optional: clearbit-style or OpenRouter + domain hint → update canonical company (shared); cache; no PII |
| **P3** | Event coalescing quality | Stronger slug + date window + location; tests with two users same event name → one canonical row, `observed_count≥2` |
| **P4** | Connector interface | `EventSourceAdapter { search(q), get(id) }` in `apps/app/lib/events/` — Luma / Meetup adapters behind env keys |
| **P5** | Link external id | New nullable columns via **new migration**: `events.external_source`, `events.external_id` (unique pair); map on commit when hint matches |
| **P6** | UI | Company/event detail shows “public” / observed badge when `promotedAt` set — no cross-user people |

**Semi-public event meaning (locked):** one shared `event` row; each user’s `entity_event` edges stay private to their people. Ten users at “DevConnect ’26” increment `observed_count`; they do **not** see each other’s contacts.

---

## 6 · Acts live (#11) — coding plan

`/acts` mock already exists. Wire for real:

| WP | Title | Notes |
|---|---|---|
| **A0** | Issue checklist refresh on #11 | |
| **A1** | `acts` table migration (append-only) | status: `drafted\|snoozed\|sent\|dismissed`; kind; targetEntityId; body; runAt; sourceInteractionId |
| **A2** | Create drafts from `ExtractionResult.actions` at commit time | In `resolution.ts` or `capture.commit` after commit — sync insert `drafted` rows (Inngest later for delayed) |
| **A3** | tRPC `acts.list` / `update` / `dismiss` | userId-scoped |
| **A4** | Replace mock `ActsClient` + home stripe with real data | Enable Send only as **mailto:** / copy — no auto-send (permission-first) |
| **A5** | Inngest (or Railway cron) for `whenHint` | Only after A1–A4 stable; dangerous-ops note in PR |
| **A6** | Entity “draft check-in” CTA | Enable → creates act draft, routes to `/acts` |

---

## 7 · Execution order (Cursor)

```
T1 sourceInteractionId          ← start immediately
T2 search deep links
T3 honest empty acts
T4/U4 mock rails gone
T5 soft-delete persist
U1 post-login → /chat
U3 non-blocking record loop
U2/U5 home cleanup + orb hint
── ship tag v0.1.3-trust ──
I0–I3 LinkedIn/vCard import MVP
I6 resolve-on-capture uses imports
I4–I5 device + Google
── ship tag v0.2-imports ──
P1 (#2) + P3 event coalesce
P4–P5 Luma/Meetup adapter + external ids
── ship tag v0.2.1-public ──
A1–A4 acts drafts live (mailto send)
A5 scheduler
── ship tag v0.3-acts ──
```

Protocol per WP: issue-first → branch `cursor/<desc>-d0df` → implement → turbo gate → PR to `staging` → no AI trailer → never touch `design/**` / eval fixtures / existing migrations.

---

## 8 · Open questions (only blockers)

| # | Ask | Default if silent |
|---|---|---|
| Q1 | Post-login always `/chat`, or keep `/` as thin status? | Always `/chat` after onboarded |
| Q2 | Allow `packages/imports` now? | No — keep under `apps/app/lib/imports` until second consumer |
| Q3 | Inngest vs Railway cron for acts? | Defer to A5; sync drafts first |
| Q4 | Twitter in v0.2 MVP? | No — URL fact only / spike |
| Q5 | Public enrichers need paid APIs (Luma/Meetup keys)? | Adapters no-op without keys; CSV/manual event link first |

---

## 9 · Out of scope for v0.2 MVP coding

- Auto-send email / Gmail send-as
- Cross-user people graph / Framing B
- Editing `design/**` without blessing
- Rewriting landing (#1/#37) in the same PRs as capture trust
- Scraping LinkedIn/Twitter HTML (ToS / ban risk) — exports + OAuth only
