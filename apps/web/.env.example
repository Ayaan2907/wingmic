# ── Database (libSQL / Turso) ────────────────────────────────────────────
# Local dev: leave blank (defaults to file:./local.db)
# Prod (Turso):
#   TURSO_DB_URL=libsql://your-db-name.turso.io
#   TURSO_AUTH_TOKEN=eyJ...
TURSO_DB_URL=
TURSO_AUTH_TOKEN=

# ── Authentication (BetterAuth) ──────────────────────────────────────────
# Generate a random 32+ byte secret: openssl rand -base64 48
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3210

# ── Email (Resend) — for magic links ─────────────────────────────────────
RESEND_API_KEY=

# ── LLM providers ────────────────────────────────────────────────────────
# Anthropic Claude is used for entity extraction.
ANTHROPIC_API_KEY=
# OpenAI is used for text-embedding-3-small (entity resolution + recall vectors).
OPENAI_API_KEY=
