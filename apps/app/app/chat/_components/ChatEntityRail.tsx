'use client';

/**
 * ChatEntityRail — desktop "in this thread" column.
 *
 * Mock fixture people were a trust killer next to live chat data (T4/U4).
 * Hidden until wired to the live thread's extracted entities. Keep the
 * export so ChatClient's desktop split layout can remount without churn.
 */

export function ChatEntityRail() {
  return null;
}
